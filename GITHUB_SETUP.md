# 🚀 Инструкция по загрузке проекта на GitHub

## Вариант 1: Через командную строку (Terminal)

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите зеленую кнопку **"New"** (или "+")
3. Заполните:
   - **Repository name:** `repetitor-pod-rukoy-mvp`
   - **Description:** AI-репетитор для школьников - MVP
   - **Visibility:** Private (или Public - на ваш выбор)
   - ❌ НЕ добавляйте README, .gitignore, license (уже есть в проекте)
4. Нажмите **"Create repository"**

### Шаг 2: Откройте Terminal в VS Code

1. Откройте папку проекта `repetitor-mvp` в VS Code
2. Откройте терминал: `Terminal` → `New Terminal` (или Ctrl+`)

### Шаг 3: Выполните команды

```bash
# 1. Убедитесь что вы в папке проекта
cd /path/to/repetitor-mvp

# 2. Настройте Git (если еще не настроили)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# 3. Добавьте все файлы
git add .

# 4. Создайте первый коммит
git commit -m "Initial commit: MVP Репетитор Под Рукой"

# 5. Переименуйте ветку в main (если нужно)
git branch -M main

# 6. Добавьте удаленный репозиторий
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/repetitor-pod-rukoy-mvp.git

# 7. Загрузите на GitHub
git push -u origin main
```

### Шаг 4: Введите учетные данные

При первой загрузке Git попросит логин и пароль:
- **Username:** ваш GitHub username
- **Password:** используйте **Personal Access Token** (не обычный пароль!)

#### Как получить Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (все галочки в repo)
4. Generate token
5. **Скопируйте токен** (он показывается только раз!)
6. Используйте его как пароль

---

## Вариант 2: Через VS Code UI (визуальный интерфейс)

### Шаг 1: Откройте проект в VS Code

1. File → Open Folder
2. Выберите папку `repetitor-mvp`

### Шаг 2: Инициализируйте Git

1. Нажмите на иконку **Source Control** (слева, третья сверху)
2. Нажмите **"Initialize Repository"**

### Шаг 3: Создайте коммит

1. В Source Control увидите все файлы
2. Нажмите **"+"** рядом с "Changes" (добавить все файлы)
3. Введите сообщение коммита: `Initial commit: MVP Репетитор Под Рукой`
4. Нажмите **✓ Commit**

### Шаг 4: Создайте репозиторий на GitHub

1. В Source Control нажмите **"Publish to GitHub"**
2. Выберите **Private** или **Public**
3. Выберите файлы для загрузки (все по умолчанию)
4. Нажмите **OK**

VS Code автоматически создаст репозиторий и загрузит все файлы!

---

## Вариант 3: Через GitHub Desktop

### Шаг 1: Установите GitHub Desktop

Скачайте с https://desktop.github.com/

### Шаг 2: Откройте проект

1. File → Add Local Repository
2. Выберите папку `repetitor-mvp`

### Шаг 3: Создайте коммит

1. Отметьте все файлы (галочка слева)
2. Внизу введите: `Initial commit: MVP Репетитор Под Рукой`
3. Нажмите **"Commit to main"**

### Шаг 4: Опубликуйте

1. Нажмите **"Publish repository"**
2. Выберите название: `repetitor-pod-rukoy-mvp`
3. Описание: `AI-репетитор для школьников - MVP`
4. Выберите Private/Public
5. Нажмите **"Publish Repository"**

---

## 🔐 Что НЕ загружать на GitHub (уже в .gitignore)

❌ `.env` файлы (с ключами API и паролями)
❌ `node_modules/` (зависимости)
❌ База данных (`.db`, `.sqlite`)
❌ Логи (`.log`)

Эти файлы уже исключены в `.gitignore`!

---

## ✅ Проверка успешной загрузки

После загрузки:
1. Откройте https://github.com/YOUR_USERNAME/repetitor-pod-rukoy-mvp
2. Вы должны увидеть все файлы проекта
3. README.md отобразится на главной странице

---

## 📝 Дальнейшая работа с GitHub

### Добавление новых изменений:

```bash
# 1. Добавить измененные файлы
git add .

# 2. Создать коммит
git commit -m "Описание изменений"

# 3. Загрузить на GitHub
git push
```

### Или в VS Code:
1. Source Control
2. Stage Changes (+)
3. Commit (✓)
4. Sync Changes (↑)

---

## 🌐 Клонирование проекта на другой компьютер

```bash
git clone https://github.com/YOUR_USERNAME/repetitor-pod-rukoy-mvp.git
cd repetitor-pod-rukoy-mvp

# Установить зависимости
cd backend
npm install

cd ../telegram-bot
npm install
```

---

## 💡 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main

# Отменить изменения в файле
git checkout -- filename

# Посмотреть удаленные репозитории
git remote -v
```

---

## 🆘 Troubleshooting

### Проблема: "Permission denied (publickey)"
**Решение:** Используйте HTTPS вместо SSH:
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/repo.git
```

### Проблема: "Authentication failed"
**Решение:** Используйте Personal Access Token вместо пароля

### Проблема: Не видно приватного репозитория
**Решение:** Проверьте что вы вошли в правильный GitHub аккаунт

---

## 🎯 Рекомендации

1. ✅ Делайте коммиты часто (каждая логическая единица изменений)
2. ✅ Пишите понятные сообщения коммитов
3. ✅ НИКОГДА не загружайте `.env` файлы с реальными ключами
4. ✅ Используйте ветки для новых фич: `git checkout -b feature/название`
5. ✅ Делайте `git pull` перед началом работы (если работаете с командой)

---

**Готово! Ваш проект теперь на GitHub!** 🎉

Если возникнут вопросы - пишите!
