# 🚀 Инструкция по запуску MVP "Репетитор Под Рукой"

## 📋 Что вам понадобится

1. **Node.js** (версия 20+) - [скачать](https://nodejs.org/)
2. **PostgreSQL** (версия 15+) - [скачать](https://www.postgresql.org/download/)
3. **Anthropic API Key** - [получить](https://console.anthropic.com/)
4. **Telegram Bot Token** (опционально) - получить у [@BotFather](https://t.me/botfather)

---

## 📥 Шаг 1: Установка зависимостей

### Backend
```bash
cd backend
npm install
```

### Telegram Bot
```bash
cd telegram-bot
npm install
```

---

## 🔧 Шаг 2: Настройка базы данных

### 2.1 Создайте базу данных PostgreSQL

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE repetitor_db;

# Выйдите
\q
```

### 2.2 Настройте переменные окружения

Создайте файл `backend/.env`:

```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Database
DATABASE_URL="postgresql://postgres:ваш_пароль@localhost:5432/repetitor_db"

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET="ваш-супер-секретный-ключ-123456"

# Anthropic Claude API
ANTHROPIC_API_KEY="sk-ant-api03-ваш-ключ-здесь"

# Telegram (опционально)
TELEGRAM_BOT_TOKEN="1234567890:ABCdef-ваш-токен"
ADMIN_TELEGRAM_ID="ваш-telegram-user-id"

# URLs
WEB_URL="http://localhost:3000"
API_URL="http://localhost:4000"

# Node
NODE_ENV="development"
PORT="4000"
```

### 2.3 Инициализируйте базу данных

```bash
cd backend

# Сгенерируйте Prisma Client
npx prisma generate

# Примените миграции
npx prisma migrate dev --name init

# (Опционально) Откройте Prisma Studio для просмотра БД
npx prisma studio
```

---

## 🎯 Шаг 3: Запуск приложения

### 3.1 Запустите Backend API

```bash
cd backend
npm run dev
```

Вы должны увидеть:
```
🚀 Server running on port 4000
📡 WebSocket server ready
🌍 Environment: development
```

**Backend API будет доступен на:** `http://localhost:4000`

### 3.2 Откройте Frontend

Просто откройте файл в браузере:

```bash
cd frontend
open index.html  # на Mac
# или
start index.html  # на Windows
# или просто перетащите файл в браузер
```

**Frontend будет доступен в браузере!**

### 3.3 Запустите Telegram Bot (опционально)

```bash
cd telegram-bot

# Создайте .env
cp .env.example .env

# Отредактируйте .env и добавьте ваш TELEGRAM_BOT_TOKEN

# Запустите
npm run dev
```

Вы должны увидеть:
```
🤖 Telegram бот "Репетитор Под Рукой" запущен!
```

---

## 🧪 Шаг 4: Тестирование

### 4.1 Тестирование Frontend

1. Откройте `index.html` в браузере
2. Прокрутите до секции "Попробуйте прямо сейчас!"
3. Выберите предмет (например, "Математика")
4. Задайте вопрос
5. Получите ответ от AI (демо-режим)

### 4.2 Тестирование Backend API

#### Регистрация:
```bash
curl -X POST http://localhost:4000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "name": "Тестовый Ученик"
  }'
```

Вы получите токен:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {...}
}
```

#### Отправить вопрос AI:
```bash
curl -X POST http://localhost:4000/api/ai/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ВАШ_ТОКЕН_СЮДА" \
  -d '{
    "message": "Объясни теорему Пифагора",
    "subject": "Математика",
    "grade": 8,
    "outputMode": "text"
  }'
```

Вы получите ответ от Claude:
```json
{
  "text": "Отлично! Давай разберём теорему Пифагора...",
  "confidence": 0.95,
  "needsReview": false
}
```

#### Тестирование поддержки:
```bash
curl -X POST http://localhost:4000/api/support/message \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ВАШ_ТОКЕН_СЮДА" \
  -d '{
    "message": "Как изменить пароль?"
  }'
```

### 4.3 Тестирование Telegram бота

1. Найдите своего бота в Telegram
2. Отправьте `/start`
3. Выберите предмет
4. Задайте вопрос
5. Получите ответ!

---

## 🐛 Решение проблем

### Проблема: "Error: P1001: Can't reach database server"

**Решение:**
- Убедитесь что PostgreSQL запущен
- Проверьте DATABASE_URL в `.env`
- Проверьте пароль и имя базы данных

### Проблема: "Error: Invalid Anthropic API key"

**Решение:**
- Проверьте ANTHROPIC_API_KEY в `.env`
- Убедитесь что ключ начинается с `sk-ant-`
- Получите новый ключ на [console.anthropic.com](https://console.anthropic.com/)

### Проблема: Telegram бот не отвечает

**Решение:**
- Проверьте TELEGRAM_BOT_TOKEN в `.env`
- Убедитесь что бот запущен (`npm run dev`)
- Проверьте логи бота на ошибки

### Проблема: Frontend не подключается к API

**Решение:**
- Убедитесь что backend запущен на порту 4000
- Проверьте консоль браузера на CORS ошибки
- Если используете frontend на другом порту, обновите CORS настройки в `backend/src/server.js`

---

## 📊 Структура URL

- **Frontend:** `file:///путь/к/frontend/index.html`
- **Backend API:** `http://localhost:4000`
- **API Documentation:** `http://localhost:4000/health`
- **Prisma Studio:** `http://localhost:5555` (если запущен)

---

## 🎓 Следующие шаги

### Для продакшн деплоя:

1. **Frontend:** Задеплойте на Vercel/Netlify
2. **Backend:** Задеплойте на Railway/Render
3. **Database:** Используйте Supabase/Neon
4. **Telegram Bot:** Запустите на том же сервере что и backend

### Для разработки:

1. Добавьте больше предметов
2. Улучшите AI промпты
3. Добавьте голосовой режим
4. Создайте мобильное приложение
5. Добавьте геймификацию

---

## 💡 Полезные команды

```bash
# Backend
npm run dev          # Запуск в dev режиме
npm start            # Запуск в production
npx prisma studio    # Открыть Prisma Studio
npx prisma migrate   # Создать новую миграцию

# Telegram Bot
npm run dev          # Запуск в dev режиме
npm start            # Запуск в production
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в консоли
2. Проверьте `.env` файлы
3. Убедитесь что все зависимости установлены
4. Перезапустите сервисы

**Успехов с MVP!** 🚀
